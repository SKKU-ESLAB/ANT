cmake_minimum_required(VERSION 2.8)

if(CONFIG_TARGET_RASPBERRY_PI_2 STREQUAL y)
  add_subdirectory(target/raspberry-pi2_3/device-drivers)
elseif(CONFIG_TARGET_RASPBERRY_PI_3 STREQUAL y)
  add_subdirectory(target/raspberry-pi2_3/device-drivers)
elseif(CONFIG_TARGET_TEGRA_TX1 STREQUAL y)
  add_subdirectory(target/tegraTX1/device-drivers)
elseif(CONFIG_TARGET_TEGRA_TX2 STREQUAL y)
  add_subdirectory(target/tegraTX1/device-drivers)
elseif(CONFIG_TARGET_ODROID_XU3 STREQUAL y)
  add_subdirectory(target/odroid-xu4/device-drivers)
elseif(CONFIG_TARGET_ODROID_XU4 STREQUAL y)
  add_subdirectory(target/odroid-xu4/device-drivers)
elseif(CONFIG_TARGET_OTHER STREQUAL y)
  add_subdirectory(target/other/device-drivers)
endif()
